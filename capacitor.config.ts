import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'coordenadasGps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
